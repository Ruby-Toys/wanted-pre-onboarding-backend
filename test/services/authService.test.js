const {authService} = require('../../services/');

describe('signUpOfJobSeeker 테스트', () => {
    test('회원가입시 이메일 값이 비어있는 경우 예외 발생', () => {
    })

    test('회원가입시 비밀번호 값이 비어있는 경우 예외 발생', () => {
    })

    test('회원가입시 이름 값이 비어있는 경우 예외 발생', () => {
    })

    test('회원가입시 연락처 값이 비어있는 경우 예외 발생', () => {
    })

    test('회원가입시 이미 같은 이메일의 계정이 존재하는 경우 예외 발생', () => {
    })

    test('회원가입시 이메일이 중복되지 않으면 성공', () => {
    })
})

